#include <stdio.h>

int main(void)
{
	long int n = 612852475143;
	long int i;
	long int prime;

	for (i = 2; i <= n; i++)
	{
		if (n % i == 0)
		{
			prime = i;
			n /= prime;
		}
	}
	printf("%ld\n", prime);
}
